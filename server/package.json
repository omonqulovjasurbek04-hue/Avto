{
  "name": "@yhq/server",
  "version": "0.1.0",
  "private": true,
  "description": "Express REST API over the scenario content, with a JSON progress store. Runs the compiled Dart engine (via @yhq/engine) to classify answers and to serve the engine bundle to clients.",
  "type": "module",
  "main": "src/index.mjs",
  "scripts": {
    "start": "node src/index.mjs",
    "dev": "node --watch src/index.mjs"
  },
  "dependencies": {
    "@yhq/engine": "*",
    "cors": "^2.8.5",
    "express": "^4.21.2"
  }
}
