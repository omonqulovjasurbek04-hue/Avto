# Server
PORT=4000

# Auth
JWT_SECRET=your-secret-key-change-in-production

# Database (PostgreSQL - optional, falls back to JSON files)
DATABASE_URL=postgresql://postgres:password@localhost:5432/yhq?schema=public

# CORS origins (comma-separated, optional)
CORS_ORIGINS=http://localhost:3000,http://localhost:5173
